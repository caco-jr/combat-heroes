import React from 'react';

import { PreviewSlotWrapper } from './index.style';

const PreviewSlot = ({ name }) => {
  return <PreviewSlotWrapper>{name}</PreviewSlotWrapper>;
};

export default PreviewSlot;
